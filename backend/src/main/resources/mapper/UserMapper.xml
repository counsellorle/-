<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.repository.UserRepository">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.order.entity.User">
        <id column="id" property="id"/>
        <result column="phone" property="phone"/>
        <result column="password" property="password"/>
        <result column="role" property="role"/>
        <result column="avatar" property="avatar"/>
        <result column="nickname" property="nickname"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, phone, password, role, avatar, nickname, status, created_at, updated_at
    </sql>

    <!-- 保存用户 -->
    <insert id="save" parameterType="com.example.order.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (phone, password, role, avatar, nickname, status, created_at, updated_at)
        VALUES (#{phone}, #{password}, #{role}, #{avatar}, #{nickname}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.example.order.entity.User">
        UPDATE user
        SET phone = #{phone},
            password = #{password},
            role = #{role},
            avatar = #{avatar},
            nickname = #{nickname},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>
